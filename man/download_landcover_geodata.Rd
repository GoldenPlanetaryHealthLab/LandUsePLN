% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_bounding_box_for_iso_code.R
\name{download_landcover_geodata}
\alias{download_landcover_geodata}
\title{Download and crop land cover data for a country}
\usage{
download_landcover_geodata(
  country_code = "NPL",
  cover_type = "trees",
  tmp_path = tempdir(),
  plot_invisible = FALSE
)
}
\arguments{
\item{country_code}{Character scalar giving the ISO3 country code, for
example \code{"NPL"}.}

\item{cover_type}{Character scalar giving the land cover type to download,
for example \code{"trees"}.}

\item{tmp_path}{Character scalar giving the directory where the downloaded
land cover file should be stored. Defaults to \code{tempdir()}.}

\item{plot_invisible}{Logical scalar passed to
\code{crop_landcover_with_box()}. Controls whether the crop operation also
produces a plot.}
}
\value{
A cropped land cover raster object returned by
\code{crop_landcover_with_box()}.
}
\description{
Downloads a land cover raster for a specified cover type using
\code{geodata::landcover()}, retrieves the country's bounding box from its ISO3
code, and crops the raster to that extent.
}
\examples{
\dontrun{
x <- download_landcover_geodata(country_code = "NPL", cover_type = "trees")
print(x)
}
download_landcover_geodata("NPL", "grassland")
}
